services:
  discordbot-dmr:
    build: .
    container_name: discordbot-dmr
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

